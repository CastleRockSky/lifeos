# Test-only dependencies. Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest
#
# The integration tests use stdlib urllib, so pytest is the only addition.
pytest>=8,<9
